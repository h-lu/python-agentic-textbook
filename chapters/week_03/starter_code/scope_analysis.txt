# Week 03 题目 4：作用域分析

- 局部变量：在函数内部创建，只在该函数调用期间可用。函数结束后，外部代码不能直接访问它。
- 全局变量：在函数外部创建，模块内的函数可以读取；如果要在函数内重新赋值，需要显式使用 global，但入门阶段应尽量避免。
- 参数也是局部变量：调用函数时传入的值会绑定到参数名，函数外部同名变量不会被自动修改。
- 返回值是函数向外传递结果的推荐方式，比在函数里直接修改全局变量更清晰、更容易测试。
