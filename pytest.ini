[pytest]
# Several week folders intentionally contain same-named test modules
# (for example test_smoke.py). importlib mode keeps range runs isolated
# without masking test failures.
addopts = --import-mode=importlib
